require('dotenv').config(); const pool = require('./src/config/db'); pool.query('SELECT COUNT(*) FROM products LEFT JOIN categories ON products.category_id = categories.id LEFT JOIN users ON products.seller_id = users.id WHERE products.status = \'approved\' AND categories.name ILIKE \', ['Pre-Owned Watches']).then(res => { console.log(res.rows); process.exit(0); }).catch(console.error);
