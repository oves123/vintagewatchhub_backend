const { Pool } = require('pg'); const pool = new Pool({ connectionString: 'postgresql://postgres.wevuqkzmkisnalfjnwhm:oves400102%40123@aws-1-ap-south-1.pooler.supabase.com:5432/postgres' }); pool.query('INSERT INTO watchlist (user_id, product_id, folder_id) VALUES (, , ) ON CONFLICT (user_id, product_id) DO UPDATE SET folder_id = EXCLUDED.folder_id RETURNING *', [14, 27, null]).then(res => { console.log('INSERTED:', res.rows); process.exit(0); }).catch(console.error);
